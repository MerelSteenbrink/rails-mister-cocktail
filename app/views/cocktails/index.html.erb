<h1>WELKCOME TO MISTER COCKTAIL</h1>
<p>Here are the cocktails:</p>

<table>
  <% @cocktails.each do |cocktail| %>
  <tr>
    <td>
      <%= cocktail.name %>
    </td>
    <td>
      <%= link_to "see", cocktail_path(cocktail)  %>
    </td>

    <td>
      <%= link_to "delete", cocktail_path(cocktail),
        method: :delete,
        data: {confirm: "are you sure you want to delete this cocktail?"} %>
      </td>
    </tr>
    <% end%>
  </table>

  <%= link_to "create new cocktail", new_cocktail_path() %>

